/**
 * Substituído na Entrega 4 pelo componente inteligente `ConsultaRelatorioComponent`.
 * Mantido apenas como reexport de compatibilidade (a rota aponta para o novo componente).
 */
export { ConsultaRelatorioComponent as ConsultaComponent } from './consulta-relatorio.component';
